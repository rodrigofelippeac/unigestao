# Stack — unigestao (para o scribe)

Documentação do **UNI Gestão de Pessoas** (raiz: `unigestao/`). É a "memória
viva" do projeto — decisões, progresso e arquitetura são registrados aqui.

- **Formato:** Markdown em `docs/`.
- **Skill de apoio:** `doc-padroes` (gera documentos no padrão do projeto).
- **Memórias dos agentes:** `.claude/agent-memory/<agente>/MEMORY.md` (lê/atualiza).

## Memória do agente
Conhecimento do projeto: estas `.memory/rules/scribe/`. Comportamento genérico
(como documenta/cura conhecimento, doc-de-produto vs nota-de-coordenação):
persona global `scribe`.
