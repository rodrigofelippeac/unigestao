# Stack — unigestao (para o ui-designer)

Design/identidade visual do **UNI Gestão de Pessoas** — SaaS de RH, visual
profissional, corporativo e clean.

- **Implementação visual:** CSS Custom Properties (tokens) no frontend React/Vite
  (`frontend/src/styles/globals.css`).
- **Assets de marca:** `briefing/` (paleta, logos, referências).
- **Identidade documentada:** `CLAUDE.md` do projeto.

## Memória do agente
Conhecimento do projeto: estas `.memory/rules/ui-designer/`. Comportamento
genérico (como projeta layout/componentes/acessibilidade): persona global
`ui-designer`. Implementação dos tokens no código: `.memory/rules/frontend/`.
